def assign_drone(order, drones):
    # check time to each drone, assign to the closest drone
    x = 0
    return drones[x]

def closest_store(drone, stores):
    # check time to each store, assign to the closest store
    x = 0
    return stores[x]